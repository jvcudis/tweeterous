<h1>Tweets</h1>
<%= form_for Tweet.new do |t| %>
<%= t.text_field :content %>
<%= t.text_field :user_id %>
<%= t.submit 'Tweet!' %>
<% end %>
<% for tweet in @tweets %>
  <%= tweet.content %> | <%= tweet.user_id %>
<% end %>